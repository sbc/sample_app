= form_for @micropost do |f|
  = render 'shared/error_messages', :object => f.object
  .field 
    = f.text_area :content
  %span#counter
    140
  characters left.
  .actions
    = f.submit "Submit"
